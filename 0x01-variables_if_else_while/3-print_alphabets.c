#include<stdio.h>
/**
 * main - main block
 * Description:  prints the alphabet in lowercase,
 * and then in uppercase, followed by a new line.
 * Return: 0
 */
int main()
{
	char ch = 'a';
	char ch2 = 'A';

while (ch <= 'z')
{
	putchar(ch);
	ch++;

}

	while
	(ch2 <= 'Z');
{
	putchar(ch2);
	ch2++;
}

putchar('/n');

return (0);
}
